language: java

matrix:
    include:
        -   jdk: oraclejdk8
            os: linux
            dist: trusty
        -   jdk: oraclejdk11
            os: linux
        -   jdk: openjdk8
            os: linux
        -   jdk: openjdk9
            os: linux
        -   jdk: openjdk10
            os: linux
        -   jdk: openjdk11
            os: linux
        -   jdk: openjdk12
            os: linux
        -   jdk: openjdk13
            os: linux
        -   jdk: openjdk14
            os: linux

    allow_failures:
        -   jdk: openjdk-ea